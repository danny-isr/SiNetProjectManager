<Window x:Class="SiNet.App.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SiNet — new architecture (scaffold)"
        Height="320" Width="560">
    <Grid Margin="24">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="SiNet.App.Wpf host scaffold"
                       FontSize="20" FontWeight="SemiBold"
                       HorizontalAlignment="Center" />
            <TextBlock Text="Composition root wired via AddSiNet(). UI lives only in this layer."
                       Margin="0,8,0,0" Opacity="0.7"
                       HorizontalAlignment="Center" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Window>

<Window x:Class="SiNet.App.Wpf.Surfaces.Tasks.TaskPanelReadOnlyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SiNet.App.Wpf.Surfaces.Tasks"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Width="1100" Height="700"
        MinWidth="800" MinHeight="500"
        FlowDirection="RightToLeft"
        d:DataContext="{d:DesignInstance Type=local:TaskPanelReadOnlyViewModel, IsDesignTimeCreatable=True}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="200" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
            <Button Content="רענון" Command="{Binding RefreshCommand}" Margin="16,0,0,0" Padding="12,4" />
        </StackPanel>

        <TabControl Grid.Row="1">
            <TabItem Header="קצר">
                <DataGrid ItemsSource="{Binding QuickTasks}"
                          SelectedItem="{Binding SelectedTask}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Priority" Binding="{Binding WorkPriority}" Width="70" />
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                        <DataGridTextColumn Header="Task type" Binding="{Binding TaskTypeName}" Width="120" />
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusName}" Width="100" />
                        <DataGridTextColumn Header="Project" Binding="{Binding ProjectId}" Width="70" />
                        <DataGridTextColumn Header="Due" Binding="{Binding DueDate, StringFormat=d}" Width="90" />
                        <DataGridTextColumn Header="ComponentKey" Binding="{Binding ComponentKey}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="בינוני">
                <DataGrid ItemsSource="{Binding MediumTasks}"
                          SelectedItem="{Binding SelectedTask}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Priority" Binding="{Binding WorkPriority}" Width="70" />
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                        <DataGridTextColumn Header="Task type" Binding="{Binding TaskTypeName}" Width="120" />
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusName}" Width="100" />
                        <DataGridTextColumn Header="Project" Binding="{Binding ProjectId}" Width="70" />
                        <DataGridTextColumn Header="Due" Binding="{Binding DueDate, StringFormat=d}" Width="90" />
                        <DataGridTextColumn Header="ComponentKey" Binding="{Binding ComponentKey}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="ארוך">
                <DataGrid ItemsSource="{Binding LongTasks}"
                          SelectedItem="{Binding SelectedTask}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Priority" Binding="{Binding WorkPriority}" Width="70" />
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                        <DataGridTextColumn Header="Task type" Binding="{Binding TaskTypeName}" Width="120" />
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusName}" Width="100" />
                        <DataGridTextColumn Header="Project" Binding="{Binding ProjectId}" Width="70" />
                        <DataGridTextColumn Header="Due" Binding="{Binding DueDate, StringFormat=d}" Width="90" />
                        <DataGridTextColumn Header="ComponentKey" Binding="{Binding ComponentKey}" Width="120" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

        <GroupBox Grid.Row="2" Header="פרטי משימה" Margin="0,8,0,8">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock>
                        <Run Text="TaskId: " FontWeight="SemiBold" />
                        <Run Text="{Binding SelectedTask.TaskId, TargetNullValue=—}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="ProjectId: " FontWeight="SemiBold" />
                        <Run Text="{Binding SelectedTask.ProjectId, TargetNullValue=—}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Assigned: " FontWeight="SemiBold" />
                        <Run Text="{Binding SelectedTask.AssignedToUserName, TargetNullValue=—}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Bucket: " FontWeight="SemiBold" />
                        <Run Text="{Binding SelectedTask.WorkQueueBucketDisplayName, TargetNullValue=—}" />
                    </TextBlock>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock>
                        <Run Text="WorkPriority: " FontWeight="SemiBold" />
                        <Run Text="{Binding SelectedTask.WorkPriority, TargetNullValue=—}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="TaskType: " FontWeight="SemiBold" />
                        <Run Text="{Binding SelectedTask.TaskTypeCode, TargetNullValue=—}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="Status: " FontWeight="SemiBold" />
                        <Run Text="{Binding SelectedTask.StatusName, TargetNullValue=—}" />
                    </TextBlock>
                    <TextBlock>
                        <Run Text="ComponentKey: " FontWeight="SemiBold" />
                        <Run Text="{Binding SelectedTask.ComponentKey, TargetNullValue=—}" />
                    </TextBlock>
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Resolve preview (WorkSurfaceContext)" Margin="0,0,0,8">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Button Grid.Row="0"
                        Content="בדוק פתיחה / Resolve"
                        Command="{Binding ResolveCommand}"
                        HorizontalAlignment="Right"
                        Margin="0,0,0,8"
                        Padding="12,4" />
                <TextBox Grid.Row="1"
                         Text="{Binding ResolvePreview, Mode=OneWay}"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas" />
            </Grid>
        </GroupBox>

        <TextBlock Grid.Row="4" Text="{Binding StatusMessage}" Foreground="#555" />
    </Grid>
</Window>

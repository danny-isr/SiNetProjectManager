<Window x:Class="SiNet.App.Wpf.Surfaces.Tasks.TaskWorkbenchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SiNet.App.Wpf.Surfaces.Tasks"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Width="1200" Height="780"
        MinWidth="900" MinHeight="600"
        FlowDirection="RightToLeft"
        d:DataContext="{d:DesignInstance Type=local:TaskWorkbenchViewModel, IsDesignTimeCreatable=True}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="160" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
            <Button Content="רענון" Command="{Binding RefreshCommand}" Margin="16,0,0,0" Padding="12,4" />
            <Button Content="הוסף משימה" Command="{Binding ShowAddPanelCommand}" Margin="8,0,0,0" Padding="12,4"
                    Visibility="{Binding CanWrite, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Button Content="מחק משימה" Command="{Binding DeleteTaskCommand}" Margin="8,0,0,0" Padding="12,4"
                    Visibility="{Binding CanWrite, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>
        <Border Grid.Row="1" Background="#F5F5F5" Padding="8" Margin="0,0,0,8" CornerRadius="4">
            <TextBlock Text="{Binding DiagnosticsText}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
        </Border>
        <TextBlock Grid.Row="2" Text="{Binding StatusMessage}" Margin="0,0,0,8" Foreground="#333" TextWrapping="Wrap" />
        <TabControl Grid.Row="3">
            <TabItem Header="קצר">
                <DataGrid ItemsSource="{Binding QuickTasks}" SelectedItem="{Binding SelectedTask}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Priority" Binding="{Binding WorkPriority}" Width="70" />
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                        <DataGridTextColumn Header="Project" Binding="{Binding ProjectId}" Width="70" />
                        <DataGridTextColumn Header="Assigned" Binding="{Binding AssignedToUserName}" Width="100" />
                        <DataGridTextColumn Header="Task type" Binding="{Binding TaskTypeName}" Width="120" />
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusName}" Width="100" />
                        <DataGridTextColumn Header="Due" Binding="{Binding DueDate, StringFormat=d}" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="בינוני">
                <DataGrid ItemsSource="{Binding MediumTasks}" SelectedItem="{Binding SelectedTask}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Priority" Binding="{Binding WorkPriority}" Width="70" />
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                        <DataGridTextColumn Header="Project" Binding="{Binding ProjectId}" Width="70" />
                        <DataGridTextColumn Header="Assigned" Binding="{Binding AssignedToUserName}" Width="100" />
                        <DataGridTextColumn Header="Task type" Binding="{Binding TaskTypeName}" Width="120" />
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusName}" Width="100" />
                        <DataGridTextColumn Header="Due" Binding="{Binding DueDate, StringFormat=d}" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="ארוך">
                <DataGrid ItemsSource="{Binding LongTasks}" SelectedItem="{Binding SelectedTask}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Priority" Binding="{Binding WorkPriority}" Width="70" />
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                        <DataGridTextColumn Header="Project" Binding="{Binding ProjectId}" Width="70" />
                        <DataGridTextColumn Header="Assigned" Binding="{Binding AssignedToUserName}" Width="100" />
                        <DataGridTextColumn Header="Task type" Binding="{Binding TaskTypeName}" Width="120" />
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusName}" Width="100" />
                        <DataGridTextColumn Header="Due" Binding="{Binding DueDate, StringFormat=d}" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
        <GroupBox Grid.Row="4" Header="הוספת משימה" Margin="0,8,0,8"
                  Visibility="{Binding IsAddPanelVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,8">
                    <TextBlock Text="Title" FontWeight="SemiBold" />
                    <TextBox Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="8,0,0,8">
                    <TextBlock Text="Due date" FontWeight="SemiBold" />
                    <DatePicker SelectedDate="{Binding NewDueDate}" />
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,8">
                    <TextBlock Text="Project" FontWeight="SemiBold" />
                    <ComboBox ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProject}" DisplayMemberPath="DisplayName" />
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,8">
                    <TextBlock Text="Assigned to" FontWeight="SemiBold" />
                    <ComboBox ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedAssignee}" DisplayMemberPath="DisplayName" />
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,8,8">
                    <TextBlock Text="Task type" FontWeight="SemiBold" />
                    <ComboBox ItemsSource="{Binding TaskTypes}" SelectedItem="{Binding SelectedTaskType}" DisplayMemberPath="DisplayName" />
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="1" Margin="8,0,0,8">
                    <TextBlock Text="Status" FontWeight="SemiBold" />
                    <ComboBox ItemsSource="{Binding Statuses}" SelectedItem="{Binding SelectedStatus}" DisplayMemberPath="DisplayName" />
                </StackPanel>
                <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,8,0">
                    <TextBlock Text="Bucket" FontWeight="SemiBold" />
                    <ComboBox ItemsSource="{Binding Buckets}" SelectedItem="{Binding SelectedBucket}" DisplayMemberPath="DisplayName" />
                </StackPanel>
                <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <Button Content="הוסף" Command="{Binding CreateTaskCommand}" Padding="16,4" Margin="0,0,8,0" />
                    <Button Content="ביטול" Command="{Binding HideAddPanelCommand}" Padding="16,4" />
                </StackPanel>
            </Grid>
        </GroupBox>
        <GroupBox Grid.Row="5" Header="Resolve preview (WorkSurfaceContext)" Margin="0,0,0,8">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Content="בדוק פתיחה / Resolve" Command="{Binding ResolveCommand}" HorizontalAlignment="Right" Margin="0,0,0,8" Padding="12,4" />
                <TextBox Grid.Row="1" Text="{Binding ResolvePreview, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" />
            </Grid>
        </GroupBox>
    </Grid>
</Window>

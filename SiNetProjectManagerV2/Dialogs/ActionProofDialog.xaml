<Window x:Class="SiNetProjectManagerV2.Dialogs.ActionProofDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="פעולה שבוצעה"
        Width="420" Height="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        Background="{DynamicResource AppBackground}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#FFF8E1" Padding="12,8" CornerRadius="4" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="📝 מה עשית?" 
                           FontSize="15" FontWeight="Bold" 
                           Foreground="#E65100"/>
                <TextBlock Text="המשימה עוברת להמתנה — תעד מה בוצע לפני ההעברה" 
                           FontSize="11" Foreground="#666" Margin="0,4,0,0"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Label -->
        <TextBlock Grid.Row="1" Text="תיאור הפעולה:" 
                   FontSize="12" FontWeight="SemiBold" 
                   Foreground="#333" Margin="0,0,0,4"/>

        <!-- Note Input -->
        <TextBox Grid.Row="2" x:Name="NoteTextBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Padding="6"
                 FontSize="13"
                 BorderBrush="#CCC"
                 BorderThickness="1"
                 Margin="0,0,0,12"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="שמור" 
                    Click="Save_Click"
                    IsDefault="True"
                    Padding="12,6" MinWidth="70" Margin="0,0,6,0"
                    FontWeight="SemiBold"
                    Background="#E8F5E9" BorderBrush="#81C784"/>
            <Button Content="דלג" 
                    Click="Skip_Click"
                    Padding="12,6" MinWidth="70" Margin="0,0,6,0"/>
            <Button Content="ביטול" 
                    Click="Cancel_Click"
                    IsCancel="True"
                    Padding="12,6" MinWidth="70"
                    Foreground="#999"/>
        </StackPanel>
    </Grid>
</Window>

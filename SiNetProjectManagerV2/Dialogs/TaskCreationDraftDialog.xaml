<Window x:Class="SiNetProjectManagerV2.Dialogs.TaskCreationDraftDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="יצירת משימה"
        Height="460" Width="540"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        ResizeMode="CanResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="ProjectInfoText" Margin="0,0,0,8" Opacity="0.7"/>

        <TextBlock Grid.Row="1" Text="כותרת:" Margin="0,0,0,4"/>
        <TextBox Grid.Row="2" x:Name="TitleBox" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="תאריך יעד:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <DatePicker x:Name="DueDatePicker" Width="160"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Text="תיאור:" Margin="0,0,0,4"/>
        <TextBox Grid.Row="5" x:Name="BodyBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 MinHeight="160"
                 Margin="0,0,0,8"/>

        <TextBlock Grid.Row="6" x:Name="ErrorText"
                   Foreground="Red"
                   Visibility="Collapsed"
                   Margin="0,0,0,8"
                   TextWrapping="Wrap"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="צור משימה" Width="100" Margin="0,0,8,0"
                    IsDefault="True" Click="OnConfirmClick"/>
            <Button Content="ביטול" Width="80"
                    IsCancel="True" Click="OnCancelClick"/>
        </StackPanel>
    </Grid>
</Window>

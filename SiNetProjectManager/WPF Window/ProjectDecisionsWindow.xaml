<Window x:Class="SiNetProjectManager.WPF_Window.ProjectDecisionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}"
        Height="620" Width="750"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        ResizeMode="CanResizeWithGrip"
        MinHeight="450" MinWidth="550"
        Background="{DynamicResource AppBackground}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- Header -->
            <RowDefinition Height="Auto"/>   <!-- Filter + New Category -->
            <RowDefinition Height="Auto"/>   <!-- Add New -->
            <RowDefinition Height="*"/>      <!-- Decisions List -->
            <RowDefinition Height="Auto"/>   <!-- Edit Panel -->
            <RowDefinition Height="Auto"/>   <!-- Status Bar -->
        </Grid.RowDefinitions>

        <!-- ════════════════════ Row 0 — Header ════════════════════ -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="החלטות פרויקט" FontWeight="Bold"/>
            <TextBlock Text="החלטות בירושה מפרויקט אב מוצגות בכחול ואינן ניתנות לעריכה."
                       Opacity="0.7" TextWrapping="Wrap" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- ════════════════════ Row 1 — Filter + Category ════════════════════ -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="סינון קטגוריה:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding SelectedCategory}"
                      DisplayMemberPath="Name"
                      IsEditable="False"
                      VerticalAlignment="Center"/>

            <Button Grid.Column="3" Content="➕ קטגוריה חדשה"
                    Command="{Binding AddCategoryCommand}"
                    Padding="8,4" VerticalAlignment="Center"/>
        </Grid>

        <!-- ════════════════════ Row 2 — Add New Decision ════════════════════ -->
        <GroupBox Grid.Row="2" Header="הוספת החלטה חדשה" Margin="0,0,0,8"
                  Visibility="{Binding IsNotEditing, Converter={StaticResource BoolToVis}}">
            <Grid Margin="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="קטגוריה:" VerticalAlignment="Center" Margin="0,0,6,4"/>
                <ComboBox Grid.Row="0" Grid.Column="1"
                          ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding NewDecisionCategory}"
                          DisplayMemberPath="Name"
                          Margin="0,0,0,4"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="תוכן:" VerticalAlignment="Top" Margin="0,0,6,4"/>
                <TextBox Grid.Row="1" Grid.Column="1"
                         Text="{Binding NewContent, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" TextWrapping="Wrap"
                         Height="60" VerticalScrollBarVisibility="Auto"
                         Margin="0,0,0,4"/>

                <Button Grid.Row="2" Grid.Column="1"
                        Content="הוסף החלטה"
                        Command="{Binding AddCommand}"
                        HorizontalAlignment="Left"
                        Padding="12,4"/>
            </Grid>
        </GroupBox>

        <!-- ════════════════════ Row 3 — Decisions List ════════════════════ -->
        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding Decisions}"
                  SelectedItem="{Binding SelectedDecision}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  BorderBrush="#DDD" BorderThickness="1"
                  RowHeaderWidth="0"
                  Margin="0,0,0,8">

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsInherited}" Value="True">
                            <Setter Property="Background" Value="#E8F0FE"/>
                            <Setter Property="FontStyle" Value="Italic"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="קטגוריה" Binding="{Binding CategoryName}" Width="100"/>
                <DataGridTextColumn Header="תוכן" Binding="{Binding Content}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="נוצר ע״י" Binding="{Binding CreatedByName}" Width="80"/>
                <DataGridTextColumn Header="תאריך" Binding="{Binding CreatedAt, StringFormat=dd/MM/yy HH:mm}" Width="100"/>
                <DataGridTemplateColumn Header="מקור" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                        <Setter Property="Text" Value="עצמי"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsInherited}" Value="True">
                                                <Setter Property="Text" Value="בירושה"/>
                                                <Setter Property="Foreground" Value="#1565C0"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>

            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="ערוך" Click="EditDecision_Click"
                              IsEnabled="{Binding PlacementTarget.DataContext.CanEditSelected, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                    <MenuItem Header="מחק" Command="{Binding DeleteCommand}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>

        <!-- ════════════════════ Row 4 — Edit Panel ════════════════════ -->
        <GroupBox Grid.Row="4" Header="עריכת החלטה (גרסה קודמת נשמרת אוטומטית)" Margin="0,0,0,8"
                  Visibility="{Binding IsEditing, Converter={StaticResource BoolToVis}}">
            <Grid Margin="6">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="קטגוריה:" VerticalAlignment="Center" Margin="0,0,6,4"/>
                <ComboBox Grid.Row="0" Grid.Column="1"
                          ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding EditCategory}"
                          DisplayMemberPath="Name"
                          Margin="0,0,0,4"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="תוכן:" VerticalAlignment="Top" Margin="0,0,6,4"/>
                <TextBox Grid.Row="1" Grid.Column="1"
                         Text="{Binding EditContent, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" TextWrapping="Wrap"
                         Height="80" VerticalScrollBarVisibility="Auto"
                         Margin="0,0,0,4"/>

                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                    <Button Content="שמור" Command="{Binding SaveEditCommand}" Padding="12,4" Margin="0,0,6,0"/>
                    <Button Content="ביטול" Command="{Binding CancelEditCommand}" Padding="12,4"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- ════════════════════ Row 5 — Status Bar ════════════════════ -->
        <StackPanel Grid.Row="5" Orientation="Horizontal">
            <TextBlock Text="{Binding StatusMessage}" Opacity="0.7" VerticalAlignment="Center"/>
            <Button Content="🔄 רענון" Command="{Binding RefreshCommand}"
                    Margin="12,0,0,0" Padding="8,2" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>

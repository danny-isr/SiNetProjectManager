<Window x:Class="SiNetProjectManager.WPF_Window.StatusMappingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="מיפוי סטטוס משימה → סטטוס פרויקט"
        Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        ResizeMode="CanResizeWithGrip"
        MinHeight="350" MinWidth="450"
        Background="{DynamicResource AppBackground}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="מיפוי סטטוס משימה → סטטוס פרויקט" FontWeight="Bold"/>
            <TextBlock Text="כאשר סטטוס משימה משתנה, הפרויקט יעודכן אוטומטית לסטטוס המתאים. השאר ריק כדי לא למפות." 
                       Opacity="0.7" TextWrapping="Wrap" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Mapping Grid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding MappingRows}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserSortColumns="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  BorderBrush="#DDD"
                  BorderThickness="1"
                  RowHeaderWidth="0">
            <DataGrid.Columns>
                <!-- Task Status Name (read-only) -->
                <DataGridTextColumn Header="סטטוס משימה" 
                                    Binding="{Binding TaskStatusName}" 
                                    Width="*"
                                    IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="4,2"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Open/Closed indicator -->
                <DataGridTextColumn Header="סוג" Width="60" IsReadOnly="True">
                    <DataGridTextColumn.Binding>
                        <Binding Path="IsOpen" StringFormat="{}">
                            <Binding.Converter>
                                <local:BoolToOpenClosedConverter xmlns:local="clr-namespace:SiNetProjectManager.Converters"/>
                            </Binding.Converter>
                        </Binding>
                    </DataGridTextColumn.Binding>
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Project Status Selector (editable) -->
                <DataGridTemplateColumn Header="סטטוס פרויקט מתאים" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.ProjectStatuses, RelativeSource={RelativeSource AncestorType=Window}}"
                                      SelectedItem="{Binding SelectedProjectStatus, UpdateSourceTrigger=PropertyChanged}"
                                      DisplayMemberPath="Title"
                                      Margin="2"
                                      VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Mapped indicator -->
                <DataGridTemplateColumn Header="" Width="30" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="✓" Foreground="Green" FontWeight="Bold"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Visibility="{Binding HasMapping, Converter={StaticResource BoolToVis}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10,0,0">
            <Button Content="💾 שמור הכל" 
                    Command="{Binding SaveCommand}"
                    Padding="12,6" MinWidth="100" FontWeight="Bold"/>
            <Button Content="רענן" 
                    Command="{Binding RefreshCommand}"
                    Padding="8,6" MinWidth="70" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Status Bar -->
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" 
                   Opacity="0.7" Margin="0,8,0,0"/>
    </Grid>
</Window>

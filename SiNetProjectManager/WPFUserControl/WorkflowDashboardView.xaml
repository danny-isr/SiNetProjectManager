<UserControl x:Class="SiNetProjectManager.WPFUserControl.WorkflowDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             FontFamily="{DynamicResource AppFontFamily}"
             FontSize="{DynamicResource AppFontSize}"
             Background="{DynamicResource AppBackground}"
             Foreground="{DynamicResource AppForeground}"
             FlowDirection="RightToLeft"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="📊 לוח תהליכי עבודה" FontWeight="Bold"
                   FontSize="16" Margin="0,0,0,12"/>

        <!-- Filters -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="פרויקט:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox ItemsSource="{Binding Projects}"
                      SelectedItem="{Binding SelectedProject}"
                      DisplayMemberPath="Title"
                      Width="300" Margin="0,0,16,0"/>

            <CheckBox Content="הצג פעילים בלבד"
                      IsChecked="{Binding ShowActiveOnly}"
                      VerticalAlignment="Center" Margin="0,0,16,0"/>

            <Button Content="🔄 רענן" Command="{Binding RefreshCommand}"
                    Padding="8,4" Margin="0,0,8,0"/>

            <Border BorderThickness="1,0,0,0" BorderBrush="#E0E0E0" Margin="8,0,8,0"/>

            <TextBlock Text="תבנית:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox ItemsSource="{Binding Definitions}"
                      SelectedItem="{Binding SelectedDefinition}"
                      DisplayMemberPath="Name"
                      Width="180" Margin="0,0,8,0"/>
            <Button Content="▶ הפעל תהליך" Command="{Binding StartWorkflowCommand}"
                    Padding="8,4" Background="#C8E6C9" BorderBrush="#66BB6A" FontWeight="Bold"/>
        </StackPanel>

        <!-- Workflow instances grid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Instances}"
                  SelectedItem="{Binding SelectedInstance}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  BorderThickness="1"
                  BorderBrush="#E0E0E0"
                  RowHeaderWidth="0"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="תבנית" Binding="{Binding WorkflowDefinition.Name}" Width="150"/>
                <DataGridTextColumn Header="שלב נוכחי" Binding="{Binding CurrentStage.Name}" Width="150"/>
                <DataGridTextColumn Header="סטטוס" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="נוצר" Binding="{Binding CreatedAtUtc, StringFormat=dd/MM/yyyy HH:mm}" Width="130"/>
                <DataGridTextColumn Header="הערות" Binding="{Binding Notes}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status bar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="Gray"/>
        </StackPanel>
    </Grid>
</UserControl>

<Window x:Class="SiNetProjectManager.WPF.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Splash" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True" ShowInTaskbar="False">

    <Border Background="White" CornerRadius="10" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- לוגו במרכז -->
            <Image Source="/Assets/logo_si.jpg" Height="160" Width="350" HorizontalAlignment="Center"/>

            <!-- טקסט טוען -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <TextBlock Text="טוען את האפליקציה..." FontSize="20" HorizontalAlignment="Center" Margin="0,10"/>
                <ProgressBar IsIndeterminate="True" Height="10" Margin="0,20,0,0"/>
            </StackPanel>

            <!-- שם משתמש בפינה -->
            <TextBlock Grid.Row="2" x:Name="UsernameText"
                       HorizontalAlignment="Right" VerticalAlignment="Bottom"
                       FontSize="12" Margin="0,10,0,0" Foreground="Gray"/>
        </Grid>
    </Border>
</Window>

